#include <stdio.h>

int main(int argc, char** argv) {
    (void)argc; (void)argv;
    printf("AMD GPU Debugger PoC skeleton.\n");
    printf("Wire in device setup, BO management, trap handler, and CLI here.\n");
    return 0;
}
